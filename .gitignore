# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
*.local.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
# Lighthouse CI reports (npx @lhci/cli autorun — see lighthouserc.cjs); uploaded as a CI artifact.
.lighthouseci

# Local death-map bake for dev preview (see src/api/deathMap.ts). The real one is published to
# the data branch by the nightly harvest.
public/death-map.json
